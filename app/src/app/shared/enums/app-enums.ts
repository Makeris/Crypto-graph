export enum PriceType {
  Ask = 'ask',
  Bid = 'bid',
  Last = 'last',
}

export enum Subscription {
  SUBSRIPTION_TYPE = 'l1-subscription',
}

export enum ToasterType {
  ERROR = 'error',
  INFO = 'info',
}

export enum Units {
  MINUTE = 'minute',
}

export enum TokenTypes {
  BEARER = 'Bearer',
}
